# Sularchi Workspace

This repository contains the Sularchi mobile app project.

## Project Structure

- `app/sularchi/` — Expo React Native application source code

## Getting Started

1. Open terminal in `app/sularchi`
2. Install dependencies:
   - `npm install`
3. Start the app:
   - `npx expo start`

## Notes

- Configure environment values in `.env` as needed.
- Use Expo Go or an emulator to run the app locally.
